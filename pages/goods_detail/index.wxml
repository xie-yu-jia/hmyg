<!-- 轮播图 -->
<view class="swiper_wrap">
    <swiper class="swiper_list" indicator-dots autoplay circular>
        <swiper-item class="swiper_item" wx:for="{{goodsInfo.pics}}" wx:key="pics_id">
            <image class="image" src="{{item.pics_mid}}" mode="widthFix"/>
        </swiper-item>
    </swiper>
</view>
